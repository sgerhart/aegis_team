# Legacy Workspace Archive

This folder contains documents that used to live at the top level of `aegis_teams`.

They are retained for historical reference only. Current planning should happen in:

- `../../plans/`
- `../../work_orders/`

Durable AegisFlux implementation docs belong in `../../../aegisflux/`. Durable Clarion docs belong in `../../../clarion/`.

Archived here:

- `CONSOLIDATION_PLAN.md`
- `agent/`
- `backend/`
- `integration/`
